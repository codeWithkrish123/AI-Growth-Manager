['Backend deployed to Render/Railway/Fly.io with auto-deploy on push to main\n- [ ] Frontend deployed to Vercel with preview deployments on PRs\n- [ ] PostgreSQL on Neon/Supabase/RDS with automated backups + PITR\n- [ ] Redis on Upstash (free tier → paid as needed)\n- [ ] Custom domain + SSL (Cloudflare/Vercel managed)\n- [ ] DNS: `api.yourdomain.com` → backend, `app.yourdomain.com` → frontend\n- [ ] CDN for static assets (Vercel/Cloudflare)\n\n### Security\n\n- [ ] All secrets in server environment (never `.env` in repo)\n- [ ] `.env` in `.gitignore` (verify!)\n- [ ] Helmet CSP headers correct for Shopify embedded app\n- [ ] CORS locked to production frontend domain only\n- [ ] Rate limiting: 60 req/min/shop (auth: 20/15min, webhooks: 200/min)\n- [ ] Input validation on all endpoints (Zod/Joi)\n- [ ] SQL injection prevention (parameterized queries — already using)\n- [ ] Encryption at rest: access tokens AES-256 (already)\n- [ ] Dependency audit: `npm audit` clean (run weekly)\n- [ ] Sentry/error tracking configured\n\n### Reliability\n\n- [ ] Health endpoints: `/health` (liveness + readiness) for k8s/Render\n- [ ] Graceful shutdown on SIGTERM (drain connections)\n- [ ] BullMQ workers: concurrency limits, retry/backoff, dead letter queue\n- [ ] Webhook idempotency (Shopify webhook ID dedup — already)\n- [ ] Database connection pooling (pg pool — already)\n- [ ] Circuit breaker for external APIs (OpenAI, Shopify, Resend, Facebook)\n\n### Observability\n\n- [ ] Structured JSON logging (pino — already)\n- [ ] Request/response logging with correlation IDs\n- [ ] Metrics: request latency p50/p95/p99, error rate, queue depth\n- [ ] Alerts: error rate > 1%, sync failure > 5%, webhook processing lag > 60s\n- [ ] Uptime monitoring (Pingdom/Better Uptime) on `/health`\n\n### Data & Compliance\n\n- [ ] GDPR webhooks: `customers/data_request`, `customers/redact`, `shop/redact`\n- [ ] Privacy Policy page linked in footer + App Store listing\n- [ ] Terms of Service page linked in footer + App Store listing\n- [ ] Data retention policy documented (e.g., 12 months health history)\n- [ ] Shopify scopes minimal: only what app uses\n- [ ] No PII in logs (verify: emails, tokens redacted)\n- [ ] Backup/restore tested (quarterly drill)\n\n### Shopify App Store Specific\n\n- [ ] Embedded app works in Shopify Admin iframe (no X-Frame-Options issues)\n- [ ] `Content-Security-Policy: frame-ancestors https://*.myshopify.com https://admin.shopify.com`\n- [ ] App icon (1024x1024), screenshots (16:9), demo video (< 2 min)\n- [ ] OAuth callback URL exact match in Partner Dashboard\n- [ ] Uninstall webhook deactivates merchant (already coded)\n- [ ] App does not request unused scopes\n- [ ] No `console.log` leaking secrets in production build\n- [ ] Frontend bundle: no `VITE_` prefixed secrets']
"# AI Growth Manager - Comprehensive Strategic Plan" 
--- 
 
"" 
"## Phase 2: Monetization -- Plan Gating & Billing (Weeks 3-5)" 
"### Stripe Integration (Backend)" 
